# Attach to the sparkmux-owned server from a real terminal (tmux 3.2+)
# The desktop app owns -L sparkmux; this is for prefix/copy-mode, not a popup TUI.
bind-key S run-shell "tmux -L sparkmux attach-session"
